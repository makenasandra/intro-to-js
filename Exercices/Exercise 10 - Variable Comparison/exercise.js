const firstYearCompleted = true;
const yearsCompleted = 1;

yearsCompleted == firstYearCompleted 
//type casting is done in order to compare the two variables and they found to true
yearsCompleted === firstYearCompleted 
//It does not compare two different data types.Type casting is done thus result is dalse since there variables are of two different data types